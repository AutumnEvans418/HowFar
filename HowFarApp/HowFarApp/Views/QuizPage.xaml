<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"

             xmlns:views="clr-namespace:HowFarApp.Views;assembly=HowFarApp"
             Title="Quiz"
             x:Class="HowFarApp.Views.QuizPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Quiz"></Label>
            <ListView ItemsSource="{Binding Quiz.Answers}" BackgroundColor="AliceBlue" HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Question.QuestionText}"></Label>
                                <Entry Text="{Binding UserInput}"></Entry>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Finish Quiz" HorizontalOptions="End" Clicked="FinishQuizClick"></Button>
        </StackLayout>
       
    </ContentPage.Content>
</ContentPage>